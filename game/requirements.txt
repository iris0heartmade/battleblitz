# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Data validation - pin to 2.10.x for aarch64 (Raspberry Pi) wheels
pydantic==2.10.3

# Async ORM + SQLite driver
sqlalchemy>=2.0.25
aiosqlite>=0.19.0

# Health monitoring (psutil for HEALTH | line per logging standard §16)
psutil>=5.9.0

# Optional: env vars for Pydantic settings
# python-dotenv>=1.0.0

# ─── Development / test dependencies ──────────────────────────
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
# (kept in a separate file; pin to the same versions CI uses)
