# Test & dev tooling. Install alongside requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.27.0          # ASGI in-process client (already pulled by FastAPI)
