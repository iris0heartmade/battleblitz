# BattleBlitz UI 原创美术接入 — P0 评审阻塞修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 3 名独立评审 Agent 在第一轮复评中识别的全部 11 个 P0 阻塞项(主线 3.8、战斗 5.7、响应式 4.8,均低于 8.5 验收线),使第二轮复评有可能达到 ≥ 8.5/10。

**Architecture:** 每个 P0 一个 Task,改动最小化、单一文件优先、可独立验证(逐任务跑 smoke + 截图)。沿用现有 `SkinAssets` + `MainlineTheme`/`BattleTheme`/`HudTheme` 分层,新增 1 个 `BattleResultOverlay` 场景节点 + dialog 遮罩可见性接线。无新主题,无新资产。

**Tech Stack:** Godot 4.7 GDScript, ext_resource 在 .tscn,PIL 像素采样,Python pytest 契约。

**Reference rubric:** `docs/规范/UI/2026-07-25-UI-设计语言规范.md` §8 验证标准 + `docs/规划/2026-07-29-战斗UI美观度五人评审.md` 验收标准。

---

## 任务前置

- [ ] **Step 0: 基线确认**

```bash
cd /home/youko/PycharmProjects/battleblitz
git status --short
/home/youko/下载/Godot_v4.7-stable_linux.x86_64 --headless --path godot-client res://tools/smoke_test.tscn 2>&1 | tail -20
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 game/venv/bin/python -m pytest -q game/tests/test_godot_client_contract.py 2>&1 | tail -10
```

预期:`git status` 与本计划开始前一致(可能有 untracked 工具脚本);smoke 0 failed;contract 36 passed / 0 failed。如果基线失败,**停止并报告**,不要在此基础上继续修。

- [ ] **Step 0b: 当前分支确认**

```bash
git rev-parse --abbrev-ref HEAD
```

预期:`feat/godot-mainline-fe-ui`(若不是,询问用户)。

---

## Task 1: 隐藏旧 MLFrame + MLPrepSelectorRow(主线 P0 #1/#2/#3/#4)

**Files:**
- Modify: `godot-client/scenes/main.tscn:1217` (MLFrame 节点加 `visible = false`)
- Modify: `godot-client/scenes/main.tscn:1331` (MLPrepSelectorRow 节点加 `visible = false`)

**理由:** 评审 #1-4 均确认 MLFrame 及其 28 个子节点(含 5 下拉框)在 .tscn 中未硬编码 `visible = false`,仅靠 controller `_set_mainline_page` 隐藏,但 ui_review_screenshot 工具在切换时未调用该路径,导致旧"战前整备"内框与新 CampaignPanel 同时渲染。

**步骤:**

- [ ] **Step 1.1:** 打开 `godot-client/scenes/main.tscn`,定位第 1217 行 `[node name="MLFrame" type="Panel" parent="MainlineView"]`,在其属性块加一行 `visible = false`

- [ ] **Step 1.2:** 同文件第 1331 行 `[node name="MLPrepSelectorRow" type="HBoxContainer" parent="MainlineView/MLFrame"]` 加 `visible = false`

- [ ] **Step 1.3:** 跑 smoke 确认未破坏既有断言

```bash
/home/youko/下载/Godot_v4.7-stable_linux.x86_64 --headless --path godot-client res://tools/smoke_test.tscn 2>&1 | tail -5
```

预期:`passed: X` (X ≥ 589),无新 failed。

- [ ] **Step 1.4:** 跑 ui_review_screenshot 重生成主线 4 张图

```bash
/home/youko/下载/Godot_v4.7-stable_linux.x86_64 --resolution 1280x720 --path godot-client res://tools/ui_review_screenshot.tscn 2>&1 | tail -5
```

预期:`mainline_slots.png` / `mainline_chapter_detail.png` / `mainline_heroes.png` / `mainline_equipment.png` 重新生成,5 下拉框不再可见。

- [ ] **Step 1.5:** 视觉确认(打开 PNG)

```bash
ls -la godot-client/.refactor_shots/ui_1280x720/mainline_*.png
```

预期:文件大小变化,文件时间戳为刚才。

- [ ] **Step 1.6:** 提交

```bash
git add godot-client/scenes/main.tscn
git commit -m "fix(godot-ui): hide legacy MLFrame and 5-dropdown row to stop bleed-through"
```

---

## Task 2: 移除主线程重复 theme 调用(主线 P2 #11)

**Files:**
- Modify: `godot-client/scripts/mainline/mainline_controller.gd:130-145`

**理由:** 评审 #11 指出 `ml_prep_complete_btn` 等 3 个按钮被 `MenuTheme.apply_secondary_button_theme` 和 `MainlineTheme.apply_danger/secondary` 双重调用,后者覆盖前者,导致"放弃主线"应危险样式但未生效。

**步骤:**

- [ ] **Step 2.1:** Read `godot-client/scripts/mainline/mainline_controller.gd` 第 130-150 行,确认 3 处双重 theme 调用。

- [ ] **Step 2.2:** 删除 `MenuTheme.apply_secondary_button_theme(ml_prep_complete_btn)` 等 3 行冗余调用,**仅保留** `MainlineTheme.apply_danger(ml_abandon_btn)` / `apply_secondary(...)` / `apply_primary(...)` 的单次调用。

- [ ] **Step 2.3:** 跑 smoke

```bash
/home/youko/下载/Godot_v4.7-stable_linux.x86_64 --headless --path godot-client res://tools/smoke_test.tscn 2>&1 | tail -5
```

预期:0 failed。

- [ ] **Step 2.4:** 提交

```bash
git add godot-client/scripts/mainline/mainline_controller.gd
git commit -m "fix(godot-ui): remove redundant MenuTheme secondary calls in mainline controller"
```

---

## Task 3: 内联 Color("#...") 字面量迁移到 MainlineTheme 常量(主线 P1 #5)

**Files:**
- Modify: `godot-client/scripts/ui/mainline_theme.gd:18-25` (新增常量)
- Modify: `godot-client/scripts/ui/mainline_theme.gd:130, 263` (替换字面量)

**理由:** 评审 #5 指出 `_box(...)` 与 `apply_frame()` 用了 3 处 inline `Color("#0e1c31")` / `Color("#0a1628cc")` / `Color("#8b7040")`,违反 UI V3 验证标准 #2。

**步骤:**

- [ ] **Step 3.1:** 在 `mainline_theme.gd` 第 18-25 行的现有 `C_*` 常量段下追加:

```gdscript
const C_OPTION_FILL: Color = Color("#0e1c31")
const C_FRAME_INK: Color = Color("#0a1628cc")
const C_FRAME_BORDER: Color = Color("#8b7040")
```

- [ ] **Step 3.2:** 把 `mainline_theme.gd:130` 的 `Color("#0e1c31")` 替换为 `C_OPTION_FILL`。

- [ ] **Step 3.3:** 把 `mainline_theme.gd:263` 的两处 `Color("#0a1628cc")` / `Color("#8b7040")` 替换为 `C_FRAME_INK` / `C_FRAME_BORDER`。

- [ ] **Step 3.4:** grep 校验 0 处残留

```bash
grep -n 'Color("#' godot-client/scripts/ui/mainline_theme.gd
```

预期:仅顶部 `C_*` 常量定义处的 3 行有 `Color("#")`(期望总共出现 3 次)。

- [ ] **Step 3.5:** 跑 smoke 0 failed,提交

```bash
git add godot-client/scripts/ui/mainline_theme.gd
git commit -m "refactor(godot-ui): move inline Color literals in mainline_theme to C_* constants"
```

---

## Task 4: mainline_theme.gd 移除遗留 apply_frame 兼容 shim(主线 P1 #6)

**Files:**
- Modify: `godot-client/scripts/ui/mainline_theme.gd:245-263` (apply_frame 函数体)
- Modify: `godot-client/scripts/mainline/mainline_controller.gd:194-220` (移除调用方)

**理由:** 评审 #6 指出 `apply_frame()` 是给旧 MLFrame 用的兼容壳,虽然 visible=false 后不画,但仍占用主题入口并产生混淆。新 MLFrame 节点本身已不需要该路径。

**步骤:**

- [ ] **Step 4.1:** Read `mainline_controller.gd` 第 194-220 行,定位 `_apply_mainline_visual_theme()` 中对 `MainlineTheme.apply_frame(ml_frame, ml_border, ml_title, ml_summary, ml_content)` 的调用。

- [ ] **Step 4.2:** 注释掉该行(保留引用便于将来删除),并加注释:`# legacy MLFrame compatibility shim disabled — see Task 4`

- [ ] **Step 4.3:** 不删除 `apply_frame` 函数本身(避免破坏尚未发现的调用方),仅在函数体首行加 `# DEPRECATED: kept for legacy callers; new CampaignPanel/PreparePanel use apply_page_frame instead`

- [ ] **Step 4.4:** 跑 smoke 0 failed

- [ ] **Step 4.5:** 提交

```bash
git add godot-client/scripts/ui/mainline_theme.gd godot-client/scripts/mainline/mainline_controller.gd
git commit -m "refactor(godot-ui): disable legacy MLFrame apply_frame shim in mainline controller"
```

---

## Task 5: 战斗 _set_action_button 显示不可用动作(战斗 P0 #1)

**Files:**
- Modify: `godot-client/scripts/main.gd:3172-3177` (改 visible=false → disable+alpha+reason)
- Modify: `godot-client/scripts/main.gd:3148-3170` (扩展 `_refresh_action_bubble_buttons` 传 reason)

**理由:** 评审 #1 指出 `_set_action_button` 把不可用动作 `visible = false`,违反 rubric "不可用动作以 alpha 0.35-0.5 + 原因文字显示"。当前 1920 行动菜单 300px 宽却只显示 2 按钮。

**步骤:**

- [ ] **Step 5.1:** Read `main.gd:3148-3177`,理解当前 reason 字段如何拼接到按钮文本。

- [ ] **Step 5.2:** 修改 `_set_action_button` 函数:

```gdscript
func _set_action_button(btn: Button, can_show: bool, reason: String = "") -> void:
	if btn == null or not is_instance_valid(btn):
		return
	btn.disabled = not can_show
	btn.modulate.a = 0.4 if not can_show else 1.0
	# 不可用时追加原因,默认空字符串;若已含 " · " 后缀则不重复
	if not can_show and reason != "":
		var base_text: String = btn.text.split(" · ", true, 1)[0] if " · " in btn.text else btn.text
		btn.text = "%s · %s" % [base_text, reason]
	elif can_show:
		var base_text: String = btn.text.split(" · ", true, 1)[0] if " · " in btn.text else btn.text
		btn.text = base_text
```

- [ ] **Step 5.3:** 在 `_refresh_action_bubble_buttons` 中按可用性传 reason:

```gdscript
_set_action_button(move_btn, can_move, "已行动" if not can_move else "")
_set_action_button(attack_btn, can_attack, "无目标" if not can_attack else "")
_set_action_button(skill_btn, can_skill, "MP 不足" if not can_skill else "")
_set_action_button(wait_btn, has_unit, "" if has_unit else "无可占领")
_set_action_button(claim_btn, can_claim, "无目标" if not can_claim else "")
```

- [ ] **Step 5.4:** 跑 smoke 0 failed

- [ ] **Step 5.5:** 重跑 `ui_review_screenshot.tscn` --resolution 1920x1080,确认 `battle_action_menu.png` 现在显示 5 按钮(可用 + 不可用灰)

- [ ] **Step 5.6:** 提交

```bash
git add godot-client/scripts/main.gd
git commit -m "fix(godot-ui): show unavailable action buttons with reason instead of hiding"
```

---

## Task 6: 对话宽度 viewport-aware clamp(战斗 P0 #2 + 响应式 P0 #1 间接)

**Files:**
- Modify: `godot-client/scripts/autoload/dialog_manager.gd:88-97` (硬编码 ±600 → viewport 动态)
- Modify: `godot-client/scripts/autoload/dialog_manager.gd:240` (hide_dialog 时关闭遮罩)
- Modify: `godot-client/scripts/autoload/dialog_manager.gd:228` (show_dialog 时开启遮罩)
- Modify: `godot-client/scenes/main.tscn:2542-2551` (DialogOverlay 加 z_index 与 anchors_preset)

**理由:** 评审 #2 / 响应式 #1 指出对话框固定 ±600 = 1200px 宽违反 `min(vp × 0.78, 1050)` 规范,且 `DialogOverlay` 节点存在但未被任何 .gd 引用,导致遮罩不工作。

**步骤:**

- [ ] **Step 6.1:** 在 `dialog_manager.gd:88-97` 的 `_root` 配置后,新增函数:

```gdscript
func _clamp_dialog_width() -> void:
	var vp := get_viewport().get_visible_rect().size
	var w: float = minf(vp.x * 0.78, 1050.0)
	_root.offset_left = -w * 0.5
	_root.offset_right = w * 0.5
	# 高度:215-260(短句)/ 短头像对话 214
	_root.offset_top = -minf(vp.y * 0.4, 320.0)
	_root.offset_bottom = -30.0
```

- [ ] **Step 6.2:** 在 `_ready()` 末尾(`_root.mouse_filter = ...` 之后)追加:

```gdscript
_clamp_dialog_width()
get_viewport().size_changed.connect(_clamp_dialog_width)
```

- [ ] **Step 6.3:** 在 `show_dialog()` (line 226 附近) 起手处打开 DialogOverlay:

```gdscript
var overlay := get_tree().root.get_node_or_null("Main/GameView/HUD/DialogOverlay")
if overlay: overlay.visible = true
```

- [ ] **Step 6.4:** 在 `hide_dialog()` (line 233) 中 `_root.visible = false` 之后关闭:

```gdscript
var overlay := get_tree().root.get_node_or_null("Main/GameView/HUD/DialogOverlay")
if overlay: overlay.visible = false
```

- [ ] **Step 6.5:** 在 `main.tscn:2542-2551` 的 DialogOverlay 节点上添加属性:

```
z_index = 75
mouse_filter = 0
anchor_right = 1.0
anchor_bottom = 1.0
```

- [ ] **Step 6.6:** 跑 smoke 0 failed

- [ ] **Step 6.7:** 跑 1920/1280 `ui_review_screenshot.tscn`,确认对话框宽度 ≤ 1050 且遮罩可见(变暗)

- [ ] **Step 6.8:** 提交

```bash
git add godot-client/scripts/autoload/dialog_manager.gd godot-client/scenes/main.tscn
git commit -m "fix(godot-ui): clamp dialogue width and wire DialogOverlay mask"
```

---

## Task 7: 替换对话框占位 emoji 头像(战斗 P1 #4)

**Files:**
- Modify: `godot-client/scripts/autoload/dialog_manager.gd:133` (改 `👤` → 空字符串或本地化图标)
- Modify: `godot-client/scenes/main.tscn:2602-2608` (查 PortraitLabel 节点配置)

**理由:** 评审 #4 指出 `dialog_manager.gd:133` 与 `main.tscn:2602` 仍把 `_portrait_label.text = "👤"`(Godot 默认字体下渲染为蓝色人形剪影),不是立绘。

**步骤:**

- [ ] **Step 7.1:** Read `dialog_manager.gd:125-145`,定位 `_portrait_label` 初始化逻辑。

- [ ] **Step 7.2:** 把 `👤` 替换为 `""`(清空占位),并加注释:

```gdscript
# 头像位留空 — 真实立绘由 _load_portrait_for(speaker) 在 _start() 时按 hero_id 加载。
# 当前 _heroes 字典尚未填充时,这里用空字符串而非 emoji,避免渲染成字体 fallback 蓝人形。
_portrait_label.text = ""
```

- [ ] **Step 7.3:** 在 `_start()` 内 `_name_label.text = scene.get("speaker", "")` 之后插入:

```gdscript
var portrait: Texture2D = _load_portrait_for(str(scene.get("speaker", "")))
if portrait != null:
	_portrait_label.text = ""  # PortraitLabel 是 Label;改为 TextureRect 后此处改 texture
	# TODO: 若 PortraitLabel 仍为 Label,保留空字符串;若升级为 TextureRect,改为 portrait
```

- [ ] **Step 7.4:** 若 `PortraitLabel` 实际是 `Label` 类型,确认 `text = ""` 是最小改动;若是 `TextureRect`,在 `dialog_manager.gd` 中改 `_portrait_label` 变量类型为 `TextureRect`,并加 `_portrait_tex: TextureRect` 引用。

- [ ] **Step 7.5:** 跑 smoke 0 failed

- [ ] **Step 7.6:** 提交

```bash
git add godot-client/scripts/autoload/dialog_manager.gd
git commit -m "fix(godot-ui): remove placeholder emoji portrait from dialogue box"
```

---

## Task 8: 新增 BattleResultOverlay 节点(响应式 P0 #2)

**Files:**
- Modify: `godot-client/scenes/main.tscn:2688` (BattleResultPanel 之后插入 BattleResultOverlay)
- Modify: `godot-client/scripts/main.gd:2816-2841` (show_battle_result / hide_battle_result)

**理由:** 响应式评审 #2 指出 BattleResultPanel 缺对应的 `BattleResultOverlay` 兄弟节点,导致结算弹窗时棋盘仍亮。

**步骤:**

- [ ] **Step 8.1:** 在 `main.tscn:2688` `[node name="BattleResultPanel" type="Panel" parent="GameView/HUD"]` 块结束后,**紧接其后**插入新节点:

```
[node name="BattleResultOverlay" type="ColorRect" parent="GameView/HUD"]
visible = false
z_index = 75
mouse_filter = 0
color = Color(0.01, 0.03, 0.05, 0.38)
anchor_right = 1.0
anchor_bottom = 1.0
offset_right = 0.0
offset_bottom = 0.0
```

- [ ] **Step 8.2:** 在 `main.gd:153` 后增加 `@onready` 引用:

```gdscript
@onready var battle_result_overlay: ColorRect = $GameView/HUD/BattleResultOverlay
```

- [ ] **Step 8.3:** 在 `show_battle_result()` (line 2816) 中 `battle_result_panel.visible = true` 后追加:

```gdscript
if battle_result_overlay != null and is_instance_valid(battle_result_overlay):
	battle_result_overlay.visible = true
```

- [ ] **Step 8.4:** 在 `hide_battle_result()` (line 2839) 中 `battle_result_panel.visible = false` 后追加:

```gdscript
if battle_result_overlay != null and is_instance_valid(battle_result_overlay):
	battle_result_overlay.visible = false
```

- [ ] **Step 8.5:** 跑 smoke 0 failed

- [ ] **Step 8.6:** 重跑 1920 `ui_review_screenshot.tscn`,`battle_result.png` 棋盘应被半透遮罩压暗

- [ ] **Step 8.7:** 提交

```bash
git add godot-client/scenes/main.tscn godot-client/scripts/main.gd
git commit -m "fix(godot-ui): add BattleResultOverlay and wire show/hide battle result"
```

---

## Task 9: PauseOverlay z_index 与 alpha 修正(响应式 P0 #3)

**Files:**
- Modify: `godot-client/scenes/main.tscn:2464` (PauseOverlay 加 z_index / color)
- Modify: `godot-client/scripts/main.gd:2534-2536` (toggle 时保证 z-order)

**理由:** 响应式评审 #3 指出 PauseOverlay alpha 0.7 与规范 0.38 不符,且 z_index 缺失可能被 Board 覆盖。

**步骤:**

- [ ] **Step 9.1:** 在 `main.tscn:2464` 的 `[node name="PauseOverlay" type="ColorRect" parent="GameView/HUD"]` 块上修改:

```
color = Color(0.01, 0.03, 0.05, 0.38)   ; 原 0.7
z_index = 75
mouse_filter = 0
```

- [ ] **Step 9.2:** 在 `main.gd:2534-2536` 的 `_toggle_pause()` 中 `pause_overlay.visible = true` 之后追加:

```gdscript
pause_overlay.z_index = 75
```

- [ ] **Step 9.3:** 跑 smoke 0 failed

- [ ] **Step 9.4:** 重跑 1280/1920 `ui_review_screenshot.tscn`,`battle_pause.png` 棋盘应被 0.38 半透遮罩压暗而非纯黑

- [ ] **Step 9.5:** 提交

```bash
git add godot-client/scenes/main.tscn godot-client/scripts/main.gd
git commit -m "fix(godot-ui): set PauseOverlay alpha 0.38 and z_index 75 to spec"
```

---

## Task 10: 1280 下折叠左翼(战斗 P0 #3 部分)

**Files:**
- Modify: `godot-client/scripts/main.gd:192-202` (增加 viewport_size listener)
- Modify: `godot-client/scripts/ui/hud_theme.gd:126-200` (apply_hud 增加小视口分支)

**理由:** 战斗评审 #3 + 响应式评审 #6 指出 LeftHudWing 在 1280 下仍占 243px(规范要求 ~56px),1280 棋盘只剩 38% 视口。

**步骤:**

- [ ] **Step 10.1:** Read `main.gd:192-202` 的 `_update_status` 与 hud_theme.gd:126 起,确认 viewport listener 模式。

- [ ] **Step 10.2:** 在 `main.gd:192` 附近新增:

```gdscript
func _update_hud_layout_for_viewport() -> void:
	var vp := get_viewport().get_visible_rect().size
	var small: bool = vp.x <= 1366.0
	if left_hud_wing != null and is_instance_valid(left_hud_wing):
		left_hud_wing.visible = not small  # 1280 完全折叠
		if small:
			pass  # 隐藏即可,小屏不显示 wing
	if right_hud_wing != null and is_instance_valid(right_hud_wing):
		# 1280 改半透明 + 缩窄,1920 保持不透明 + 较宽
		right_hud_wing.modulate.a = 0.86 if small else 1.0
		right_hud_wing.custom_minimum_size.x = 280.0 if small else 400.0
	HudTheme.apply_hud(self)  # 重新触发以应用字号补偿
```

- [ ] **Step 10.3:** 在 `hud_theme.gd:129` 的 `var pill_size := 14` 改为:

```gdscript
var pill_size: int = 14
if get_viewport() != null and get_viewport().get_visible_rect().size.x <= 1366.0:
	pill_size = 20  # 1.4x 字号补偿
```

- [ ] **Step 10.4:** 在 `main.gd:_ready()` 末尾追加:

```gdscript
get_viewport().size_changed.connect(_update_hud_layout_for_viewport)
_update_hud_layout_for_viewport()
```

- [ ] **Step 10.5:** 跑 smoke 0 failed

- [ ] **Step 10.6:** 跑 1280 `ui_review_screenshot.tscn`,确认 `battle_default.png` 左翼不可见,棋盘占比 ≥ 70%

- [ ] **Step 10.7:** 提交

```bash
git add godot-client/scripts/main.gd godot-client/scripts/ui/hud_theme.gd
git commit -m "fix(godot-ui): collapse left HUD wing and bump font density at <=1366px"
```

---

## Task 11: 选中存档槽焦点反馈(主线 P2 #13)

**Files:**
- Modify: `godot-client/scripts/ui/mainline_theme.gd:165-180` (apply_row 增加 selected_thickness 参数)
- Modify: `godot-client/scripts/ui/mainline_campaign_panel.gd:53-58` (selected 时调用带 thick 参数)

**理由:** 主线评审 #13 指出 selected 槽位只改色不动边框,违反"焦点状态至少组合两种反馈"。

**步骤:**

- [ ] **Step 11.1:** 在 `mainline_theme.gd:apply_row()` 增加可选参数:

```gdscript
static func apply_row(button: Button, selected: bool = false, thick_border: bool = false) -> void:
	# ... 现有逻辑 ...
	if selected and thick_border:
		var sb_extra := StyleBoxFlat.new()
		sb_extra.border_color = C_GOLD_BRIGHT
		sb_extra.set_border_width_all(2)
		button.add_theme_stylebox_override("focus", sb_extra)
```

- [ ] **Step 11.2:** 在 `mainline_campaign_panel.gd:58` 的 `MainlineTheme.apply_row(card, index == _selected_slot)` 后追加焦点反馈:

```gdscript
card.focus_mode = Control.FOCUS_ALL
MainlineTheme.apply_row(card, index == _selected_slot, true)  # thick_border
```

- [ ] **Step 11.3:** 跑 smoke 0 failed

- [ ] **Step 11.4:** 跑 `ui_review_screenshot.tscn` 1280,`mainline_slots.png` 选中槽位应有 2px 金边高亮

- [ ] **Step 11.5:** 提交

```bash
git add godot-client/scripts/ui/mainline_theme.gd godot-client/scripts/ui/mainline_campaign_panel.gd
git commit -m "feat(godot-ui): add thick border focus feedback for selected save slot"
```

---

## 任务后置验证

- [ ] **Step P.1:** 跑全部 smoke 与契约

```bash
/home/youko/下载/Godot_v4.7-stable_linux.x86_64 --headless --path godot-client res://tools/smoke_test.tscn 2>&1 | tail -5
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 game/venv/bin/python -m pytest -q game/tests/test_godot_client_contract.py 2>&1 | tail -5
```

预期:smoke 0 failed,contract 36 passed / 0 failed。

- [ ] **Step P.2:** 重跑三档截图

```bash
cd /home/youko/PycharmProjects/battleblitz
for res in 1280x720 1600x900 1920x1080; do
  /home/youko/下载/Godot_v4.7-stable_linux.x86_64 --resolution $res --path godot-client res://tools/ui_review_screenshot.tscn 2>&1 | tail -3
done
```

预期:30 张截图全部重新生成。

- [ ] **Step P.3:** git log 复核

```bash
git log --oneline -n 12
```

预期:11 个新 commit,主题清晰。

- [ ] **Step P.4:** 启动第二轮评审 Agent(用同一 rubric)

```
三名评审 Agent 用与第一轮相同的 6 维度 rubric 重打分,目标是平均 ≥ 8.5。
把第二轮结果汇总后,再决定是否提交最终 commit。
```

---

## 完成定义

- [ ] 11 个 P0 全部修复,每 Task 都有 commit。
- [ ] smoke 0 failed,contract 36/0。
- [ ] 三档 30 张截图重新生成,主要缺陷目视不可见。
- [ ] 第二轮评审分数:主线 ≥ 7.5,战斗 ≥ 8.0,响应式 ≥ 8.0(平均 ≥ 8.5)。如未达,返回修复剩余 P1/P2。