# BattleBlitz 开发路线图

> **目标**：做出类似《火焰纹章》+《高级战争》完整战棋体验的 Python 服务端
> **排除项**：美术资产（精灵图、地图 tileset、UI 图标、音效文件 — 另行处理）
> **每步完成后 Agent 必须停下来询问技术细节**，确认后再进入下一步
> **完整项目架构总览**（已实现 / 进行中 / 未实现 / 关键决策）：见 [`架构.md`](架构.md)

---

# 0. 项目状态概览（2026-06-28 审计）

## 0.1 已实现功能（✅）

- ✅ 基础回合制（移动/攻击/技能/待命/结束回合）
- ✅ 5 种地图预设 + 随机种子生成
- ✅ 4 种兵种（剑士/弓手/骑士/治疗师）+ 4 种兵种配置
- ✅ 兵种属性 + 技能模块化（`classes/units/` + `classes/units/skills/`）
- ✅ LLM AI 接入（Anthropic / OpenAI 协议双支持，可切 `llama.cpp` 本地）
- ✅ 规则 AI 兜底（无 LLM 时不会卡死）
- ✅ 端级 AI 链（连续 AI 玩家自动行动）
- ✅ 后台定时器（24h 超时跳过、废弃房间清理）
- ✅ 战报日志（移动/攻击/技能/死亡/AI 表情）
- ✅ WebUI（菜单/大厅/棋盘/气泡式交互/参考面板/FLIP 动画）
- ✅ 对话框系统（`app/web/app.js` Dialog 模块，31/31 jsdom 单测）
- ✅ 主线/战役模式（`app/mainline/` 4 步全完成，含示例 chapter_01_steel_rebellion）
- ✅ 主线存档（`PlayerProfile.mainline_progress`，按 user_name 持久化）
- ✅ 战斗预测面板（forecastSingleHit/forecastAttack，含反击/暴击）
- ✅ 反击系统（`COUNTER_DAMAGE_MULT=0.5`，连击后反击，伤害类型跟随反击方）
- ✅ 物理 + 魔法双重战斗体系（matk/mdef + attack_kind + 术士兵种）
- ✅ 中文 UI 词条（UNIT_STAT_LABEL 字典统一管理）
- ✅ 地形像素图生成（`tools/gen_terrain_tiles.py`，48×48 像素图）
- ✅ P0.4 地形增强：village/barracks/road/gate + 6 castle 子特征 + 经济系统 + claim + recruit + 7 新预设地图（commit `e8853c6`~`f541017`，2026-06-30/07-01）
- ✅ P2.3 地图任务多样化：Rout / Seize / Reach / Defend + 队伍模式 + 前端 UI（commit `d48a83b`~`1e25690`，2026-07-01）
- ✅ 死代码清理（`_check_action_budget` / `_actions_per_turn` / `create_initial_units` / `UNIT_COMPOSITIONS` 等死函数删除；commit `80628a0`）
- ✅ 大厅内队伍编辑（PATCH `/games/{id}/players/{pid}/team`，PR #9 from YoukoSaint，2026-07-02）
- ✅ 经典棋子像素头像（`game/app/web/assets/classic/`，含分割工具 `tools/split_classic_icons.py`）
- ✅ 随机地图生成规范（`docs/superpowers/specs/2026-07-01-random-map-generation-spec.md`，592 行系统设计）
- ✅ 晋升/等级系统（`app/progression/`，XP + 转职证路线）
- ✅ 玩家档案 API（`app/routes/profile.py`）
- ✅ WebSocket 基础设施（`app/protocol/v1.py` + `app/events/bus.py` + Debug WS 端点）

## 0.2 进行中（🚧）

- 🚧 生产环境 WebSocket 网关（前端 `app.js` 仍用 3s 轮询；debug_ws 端点可用，未对接客户端）
- 🚧 AI reaction 演出优化（DB 已存 reaction，前端展示链路半成品）
- 🚧 自定义地图编辑器（`/editor` 已实装 API，前端 UI 半成品）
- 🚧 随机地图生成器重构（已写规格 [`2026-07-01-random-map-generation-spec.md`](superpowers/specs/2026-07-01-random-map-generation-spec.md)，实现待启动）

## 0.3 未实现（⬜）

- ⬜ P1.1 武器耐久度 & 装备系统
- ⬜ P1.2 兵种克制扩展（剑→斧→枪 三角）
- ⬜ P2.1 迷雾战争
- ⬜ P2.2 指挥官 + CO Power
- ⬜ P3.1 支援系统
- ⬜ P3.2 地形动态（着火/冰面/昼夜/沙尘暴）
- ⬜ P3.4 战斗动画层
- ⬜ 弓手 vs 术士 反向克制（`strong_against` 留空槽位）
- ⬜ 用户认证系统（当前按 user_name 软绑定）
- ⬜ 多模型投票 / ReAct 沙盘推演

## 0.4 关键设计决策

| 决策 | 文档 |
|------|------|
| MP 系统（替代简单 MOV） | [`架构.md`](架构.md) §五 7.1 |
| 士气系统（替代纯 EXP/Level） | [`架构.md`](架构.md) §五 7.2 |
| 首玩家公平性规则 | [`架构.md`](架构.md) §五 7.3 |
| 兵种/技能模块化 | [`架构.md`](架构.md) §三 3.x |
| 事件总线（pub/sub by game_id） | [`架构.md`](架构.md) §十一 11.1 |
| 主线 JSON 剧情格式 | [`架构.md`](架构.md) §十二 12.1 |
| 晋升走 XP 路线（替代"士气满 3 星"） | [`架构.md`](架构.md) §十三 13.1 |

---

# 🏗️ 施工规则

1. **每步开始**：读取此文件，找第一个 `[ ]` 项
2. **确认前**：在聊天中输出「当前步骤：XXX」+ 该步骤的「⏸ 需要确认的技术细节」
3. **等待用户回答**后再开始编码
4. **完成后**：把 `[ ]` 改为 `[✓]`
5. **每个 P 阶段全部 ✓**：输出一份该阶段总结
6. **如果用户想跳过**：把 `[ ]` 改为 `[—]`

---

# S0 — 剧情/故事系统（优先实现）

> 要实现火焰纹章式的主线剧情，需要三个底层基础设施：
> 对话框系统 + 剧情编辑器/加载器 + 存档系统。
> 这些做好之后，写剧情就只是写 JSON 的事了。

## S0.1 对话框系统

**概述**：一个通用、可复用的对话框组件，用于剧情对话、角色说话、系统提示。

**技术要求**：
- 纯 CSS 底部弹出面板（类似火纹/AVG 对话框）
- 支持：角色名 + 角色头像占位 + 对话文本 + 逐字显示（打字机效果）
- 支持：点击任意处快速显示全文 / 点击下一步
- 支持：选项分支（最多 4 个选项，选后跳转到不同对话段落）
- 支持：动画进出（底部滑入/淡入）

**改动范围**：
- `web/index.html`：对话框容器
- `web/style.css`：对话框样式（底栏、头像框、文字区、选项按钮）
- `web/app.js`：`dialog.show(texts, opts)` 函数

**状态**：[✓] （`Dialog` 模块已实装于 `app/web/app.js`：底部对话框 + 全屏叙述；支持 dialogue/narration/choice/wait 四种 scene；打字机 40 字/秒、叙述 25 字/秒；点击/空格/Enter 推进、Esc 关闭；31/31 jsdom 单测通过）

### ⏸ 需要确认的技术细节

1. 对话框是底部弹出（火纹式）还是居中弹出（Galgame 式）？
   - 推荐：底部弹出（火纹经典样式），可选居中
2. 是否需要角色名颜色配置（不同角色名不同颜色）？
3. 打字机速度多少合适？（推荐：30-50 字/秒，中文适中）

---

## S0.2 剧情系统

**概述**：定义一套 JSON 剧情格式 + 加载器，剧情脚本驱动对话框。

**JSON 剧情格式草案**：

```json
{
  "id": "chapter_01_intro",
  "scenes": [
    {
      "type": "dialogue",
      "speaker": "云",
      "speaker_color": "#ffb84d",
      "text": "这片大陆已经被黑暗笼罩了太久……",
      "next": "scene_2"
    },
    {
      "type": "dialogue",
      "speaker": "红",
      "speaker_color": "#e85a6a",
      "text": "但今天，我们将吹响反击的号角。",
      "next": "scene_3"
    },
    {
      "type": "choice",
      "question": "你选择相信谁？",
      "choices": [
        {"text": "相信云的判断", "next": "scene_4a"},
        {"text": "跟随红的勇气", "next": "scene_4b"}
      ]
    },
    {
      "type": "dialogue",
      "id": "scene_4a",
      "speaker": "云",
      "text": "你的信任不会白费。",
      "next": "scene_5"
    },
    {
      "id": "scene_4b",
      "speaker": "红",
      "text": "这才是我认识的你！",
      "next": "scene_5"
    },
    {
      "type": "battle",
      "id": "scene_5",
      "map_preset": "mountain_pass",
      "win_condition": "rout",
      "next_story": "chapter_01_after"
    }
  ]
}
```

**剧情格式支持的类型**：
| type | 效果 |
|------|------|
| `dialogue` | 对话框显示文本，支持角色名+颜色 |
| `choice` | 显示选项分支，不同选项不同下一场景 |
| `battle` | 过渡到战斗（指定地图/兵种/胜利条件）|
| `narration` | 全屏文字叙述（无对话框边框）|
| `wait` | 等待 N 秒后自动继续（用于演出效果）|
| `condition` | 根据玩家存档数据判断分支（如"第 2 章时士气＞3"）|

**游戏流程**：游戏大厅 → 选择"剧情模式" → 按章节播放剧情 → 剧情末尾进入战斗 → 战斗胜利 → 继续剧情 → ...

**改动范围**：
- `app/game_logic.py`：剧情 JSON 加载器
- `app/routes/game.py`：`POST /games/story/{chapter_id}` 端点
- `web/app.js`：`StoryEngine` 类——驱动场景播放
- `web/index.html`：剧情模式入口按钮
- `game/stories/`：剧情 JSON 文件夹

**状态**：[✓] （4 步全完成，2026-06-27）

### ⏸ 历史技术细节（已确认）

1. JSON 剧情文件放在哪个目录？
   - ✅ `game/mainlines/*.json`（主线配置）+ `game/stories/chapter_XX/*.json`（剧情片段）
2. 战斗结束后如何继续剧情？
   - ✅ 方案 A：战斗胜利后自动播放 `next_story`（`POST /mainlines/{id}/advance`）
3. 剧情中是否支持插入"教学提示"？
   - ✅ 通过 `narration` scene 类型支持

详细实施日志见 [mainline/steps.md](mainline/steps.md)、第 1 章配置见 [`game/mainlines/chapter_01_steel_rebellion.json`](../../game/mainlines/chapter_01_steel_rebellion.json)。

---

## S0.3 存档系统

**概述**：玩家可以随时保存/读取/删除存档。每个存档记录该玩家的游戏进度。

**数据模型**：

```json
{
  "id": "save_1",
  "player_name": "玩家",
  "chapter_id": "chapter_01",
  "scene_id": "scene_5",
  "game_state_snapshot": {
    "turn": 3,
    "players": [{...}],
    "units": [{...}],
    "tiles": [{...}]
  },
  "player_data": {
    "gold": 1500,
    "units_total_kills": 47,
    "total_turns_played": 12,
    "morale_accumulated": 9
  },
  "created_at": "2026-06-25T12:00:00Z"
}
```

**改动范围**：
- `models.py`：`SaveSlot` ORM 模型（id, player_name, chapter_id, scene_id, snapshot:JSON, player_data:JSON, created_at）
- `schemas.py`：保存/读取/删除 requests + responses
- `routes/game.py`：
  - `POST /games/save` — 创建存档
  - `GET /games/saves` — 列出该玩家所有存档
  - `POST /games/saves/{id}/load` — 读取存档
  - `DELETE /games/saves/{id}` — 删除存档
- `web/app.js`：
  - 保存按钮（游戏内）
  - 读档/存档列表界面
  - 自动存档（每回合开始自动存）

**状态**：[✓]（主线存档子集已实装：见 [mainline/steps.md](mainline/steps.md) Step 2，`PlayerProfile.mainline_progress` 字段按 `user_name` 持久化主线战斗序号 + scene_id；23/23 单测通过）。自由模式通用存档 SaveSlot 暂未实装，待 P0+ 平衡后再补。

### ⏸ 需要确认的技术细节

1. 存档是绑定玩家名还是需要未来加登录？
   - 推荐：先绑定玩家名（读取时输入名字），未来再加 token 认证
2. 存档数量限制？（推荐：每个名字最多 10 个存档位 + 1 个自动存档）
3. 存档是否包含全部 game_state 快照还是只存关键数据？
   - 推荐：全部快照（省去重建逻辑），JSON 大小约 20-50KB
4. 是否支持"战斗中断存"？（战斗中随时可以存，读档后回到存的那一刻）→ 推荐：支持

---

# P0 — 核心战斗革命（优先级最高）

## P0.1 敌我分阶段 (Enemy Phase)

**概述**：实现"玩家回合 → 敌方阶段 → 结算"的标准战棋流程。

**改动范围**：
- `routes/turns.py`：改写 end_turn 逻辑，新增 `game.phase` 字段
- `models.py`：Game 加 `phase: str`（player_turn / enemy_phase / resolve）
- `game_logic.py`：AI 触发逻辑移到这里
- `routes/actions.py`：enemy_phase 期间拒绝真人操作

**状态**：[✓]（commit `f539aed`：`game.phase` 字段已实装，end_turn 自动切换 player/ai 阶段，AI 链式自动行动；前端 banner 显示当前阶段，phase-disabled 时禁止玩家操作；战报中文模板同步上线。详见 [`docs/superpowers/specs/2026-06-29-turn-phases-movement-logs-design.md`](superpowers/specs/2026-06-29-turn-phases-movement-logs-design.md)）

### ⏸ 需要确认的技术细节

1. 敌方阶段 AI 是一次性全部算完还是逐帧播放（推荐逐帧 0.8-1.2s）？
2. 敌方阶段玩家是否可以操作（如查看单位、移动镜头）？
3. 是否完全废弃现在的"混合轮流"模式？

---

## P0.2 战斗预测面板 (Combat Forecast)

**概述**：攻击前显示伤害/命中/暴击/反击预测，玩家决定是否执行。

**改动范围**：
- `web/app.js`：新增 `computeForecast(attacker, target)` 函数，复制服务端伤害公式
- `web/style.css`：预测面板卡片样式
- `web/index.html`：预测面板容器

**状态**：[✓] （commit `0f64054`：forecastSingleHit/forecastAttack 已实装，含反击/暴击预测）

### ⏸ 需要确认的技术细节

1. 预测面板是否**每次攻击**都显示还是首次后可选跳过？
2. 反击伤害是否要预估（标注"若目标存活"）？
3. 多段攻击（连击/双击）如何显示——分行还是合计？

---

## P0.3 反击系统 (Counter Attack)

**概述**：近战攻击后若目标存活且攻击者在目标射程内→目标自动反击一次。

**改动范围**：
- `routes/actions.py`：attack 端点执行完主伤害后判断反击
- `game_logic.py`：`apply_counter(attacker, defender)` 函数

**状态**：[✓] （commit `0f64054`：`routes/actions.py:339` 反击逻辑已实装，`COUNTER_DAMAGE_MULT=0.5` 伤害减半，连击后反击，复用 `attack_with_double_strike`）

### ⏸ 需要确认的技术细节

1. 反击伤害是否减半？火纹是 50%。
2. 远程单位（弓箭手）攻击近战是否会被反击？
3. 双次攻击（骑士连击技能）后反击几次——每击后都反击还是连击结束后反击一次？

---

## P0.4 地形体系增强

**概述**：增加火纹/AW 经典地形—城市、兵工厂、道路、王座、关卡。

**新增地形**：

| 地形 | 移动消耗 | 防御加成 | 特殊效果 |
|------|---------|---------|---------|
| 城市/村庄 | 1 | +1 | 每回合站立回 10% HP |
| 兵工厂 | 1 | 0 | 可购买武器/道具（未来配合经济系统）|
| 道路 | 1（但 1 MP 可走 2 格）| 0 | 快速移动 |
| 王座 | 1 | +3 | Seize 模式获胜条件目标格 |
| 关卡 | ❌ | — | 不可通行，地图边界 |
| 塔/瞭望台 | 2 | +2 | 迷雾模式视野 +3 |

**改动范围**：
- `config.py`：新增 TERRAIN 常量
- `game_logic.py`：`apply_terrain_effects()` 每回合治疗城市上的单位
- `utils.py`：道路消耗 0.5 用"1格 2 MP"模拟
- `schemas.py`、`web/app.js`：前端渲染新地形颜色
- **预设地图布局**：更新 6 个预设地图加入新地形

**状态**：[✓] （2026-07-01 全 6 phase 完工）
- Phase 1：models + 迁移（village/barracks/road/gate + 6 castle sub-features）— commit `e8853c6`
- Phase 2：地形美术 + 编辑器新工具 — commit `65d3053`
- Phase 3：道路 MP 减半（`TERRAIN_MOVE_COST` doubled）+ 关卡不可通行 — commit `5b61ee1`
- Phase 4：`Player.gold` + 每回合收入（village +50 / barracks +100 / vault +150）— commit `45b8abc`
- Phase 5：`ClaimSession` + `POST /games/{id}/claim` — commit `0ffcd6d`
- Phase 6：`POST /games/{id}/recruit` + 招募 modal + 7 个新 P0.4 地图预设 — commits `5978695` / `ad15f01` / `5c91a52`
- 测试：12 tests 全过 (`tests/test_economy.py` + `tests/test_claim.py`)

### ⏸ 历史技术细节（已确认）

1. 道路实现 — ✅ 方案 B：`TERRAIN_MOVE_COST` doubled（road=1 即 0.5 MP）；BFS 预算 `mov * 2`
2. 兵工厂 — ✅ 配 P0.4 经济系统：归属后每回合 +100 金，可招募新单位
3. 城市回血 — ✅ 占归属即可，end_of_turn 自动 +50 金；本阶段不实现回 HP（路线图原计划）
4. 预设地图 — ✅ 7 个 P0.4 展示地图覆盖 15–45 全部尺寸

---

# P1 — 核心深度

## P1.1 武器耐久度 & 装备系统

**概述**：单位持有武器，每次攻击消耗 1 点耐久，归零后武器破碎/伤害减半。

**改动范围**：
- `models.py`：Unit 加 `weapons: JSON`
- `config.py`：武器模板常量（铁剑/钢剑/银剑/必杀剑等）
- `game_logic.py`：重写 `calculate_damage` 用武器参数
- `schemas.py`：`WeaponOut` schema
- `web/app.js`：显示当前武器 + 耐久条

**状态**：[ ]

### ⏸ 需要确认的技术细节

1. 伤害公式改为火纹的 `(ATK + 武器威) - DEF` 还是保持现有乘法公式？
   - 方案 A 需要大幅重新平衡数值
2. 治疗师武器（治愈杖）是否也需要耐久？
3. 武器是否可以在单位间交换？(建议 P3 再做)

---

## P1.2 兵种克制扩展

**概述**：从目前仅两条克制线，扩展到完整的剑→斧→枪→剑武器三角。

**改动范围**：
- `config.py`：重写 `TYPE_ADVANTAGE`

**状态**：[ ]

### ⏸ 需要确认的技术细节

1. 是否要新增兵种（斧头兵、枪兵）来实现完整三角？
   - 当前只有 4 个兵种
2. 或者改用"武器类型克制"（剑/枪/斧/弓）配合武器耐久系统（P1.1）？

---

## P1.3 晋升系统 (Promotion)

**概述**：等级达阈（每 60 XP 一级，最多 10 级）+ 消耗 1 张转职证（占领城堡自动 +1）→ 晋升为高级职业，获得新能力和属性提升。

**改动范围**：
- `models.py`：Unit 加 `class_tier`（已在 progression/models.py）
- `progression/leveling.py`：PROMOTE_XP_TABLE + 升级判定
- `progression/api.py`：`POST /progression/units/{id}/promote` 端点
- `progression/schemas.py`：PromoteRequest/Result
- `web/app.js`：晋升后显示 tier 差异

**状态**：[✓] （`app/progression/` 已实装：leveling.py 等级/PROMOTE_XP_TABLE、api.py `promote_unit` 端点、schemas.py PromoteRequest/Result；经验值体系替代原路线图"士气满 3 星"条件）

### ⏸ 历史技术细节（已确认）

1. 晋升后士气是否重置为 0？ ✅ 重置为 0
2. 转职证如何获得？ ✅ 占领城堡自动 +1（apply_end_of_turn 阶段）
3. 晋升后名字是否自动改名？ ✅ 通过 `class_tier` 字段保留原 name，UI 显示 tier 前缀

---

# P2 — 高级玩点

## P2.1 迷雾战争 (Fog of War)

**概述**：按单位视野隐藏敌方位置，只有视野范围内可见。

**改动范围**：
- `models.py`：Unit 加 `vision`
- `config.py`：视野默认值
- `routes/actions.py`：move 后更新视野
- `web/app.js`：renderBoard 加迷雾遮罩逻辑
- `web/style.css`：迷雾格子样式

**状态**：[ ]

### ⏸ 需要确认的技术细节

1. 视野是每个单位独立（火纹）还是全队共享（AW）？
2. 己方单位死后视野是否会缩小？
3. 友方单位是否永远可见（建议：是）？

---

## P2.2 指挥官 + CO Power

**概述**：每个玩家选指挥官，有充能条和全队技能。

**改动范围**：
- `models.py`：Player 加 `commander_id`、`charge_bar`
- `routes/turns.py`：end_turn 时充能
- `routes/actions.py`：`POST /games/{id}/activate-power`
- `web/app.js`：显示充能条 + 释放按钮

**状态**：[ ]

### ⏸ 需要确认的技术细节

1. 4 位指挥官分别是什么特性？
   - 示例：铁壁(防×1.5)/烈焰(攻×1.3)/疾风(MP全回复)/智略(复活)
2. CO Power 是否需要冷却回合？（建议：3-6 回合）
3. AI 是否自动释放 CO Power？

---

## P2.3 地图任务多样化

**概述**：不再只有"全灭"获胜，增加 Seize/Defend/Reach/Boss 模式。

**改动范围**：
- `models.py`：Game 加 win_condition/params
- `game_logic.py`：`check_victory_by_condition()`
- `config.py`：地图预设加 win_condition
- `web/app.js`：终局显示不同文案

**状态**：[✓] （2026-07-02 全 6 phase 完工）：
- Phase 1：数据 + 迁移 + Rout 增强 + 组队聚合（Game.win_condition/win_reason/defend_turns/reach_tile_id + Player.team_id） — commit `d48a83b`
- Phase 2：Seize 模式（占对方 HQ 即胜） — commit `e38e672`
- Phase 3：Reach 模式（任意己方单位到目标格即胜） — commit `1150482`
- Phase 4：Defend 模式（撑 N 轮 = 1 team 存活 = 胜） — commit `8af8108`
- Phase 5：Team mode lobby + /games/{id}/join 含 team 参数 — commit `06ce222`
- Phase 6：前端 UI（创建表单胜利条件下拉框 + 大厅队伍聚合 chip + 胜利横幅） — commit `1e25690`
- 测试：tests/test_victory.py (21) + tests/test_team_mode.py (6)
- 后续 PR #9（YoukoSaint）：大厅内 PATCH /games/{id}/players/{pid}/team 动态队伍编辑 — commit `7674627`

### ⏸ 历史技术细节（已确认）

1. Defend 模式 — ✅ 我方全队任一成员存活到指定回合即胜；中途全队阵亡立刻判负
2. Reach 模式 — ✅ 任一己方单位到目标格即胜
3. Seize 模式 — ✅ 复用 P0.4 ClaimSession：占满对方城堡 2 回合即翻转归属；归属变化时 check_win_condition 触发 seize 胜利
4. 1V1 队伍模式默认 — ✅ _team_of(player) 在 team_id 为空时回退到 f"player_{player.id}"，避免被颜色误判为同队

---

# P3 — 锦上添花

## P3.1 支援系统 (Support)

**概述**：两个单位相邻站立累计 N 回合后，自动解锁支援等级（C/B/A），相邻时提供攻防加成。

**改动范围**：
- `models.py`：Player 加 support_levels JSON
- `game_logic.py`：`check_support_pairs()` 在 end_of_turn 调用
- `web/app.js`：显示支援关系

**状态**：[ ]

### ⏸ 需要确认的技术细节

1. 支援等级是累计还是要求连续相邻？
2. 所有组合都有效还是限制特定组合？
3. 支援效果只在相邻时触发？（建议：是）

---

## P3.2 地形动态

**概述**：森林着火、冰面破裂、昼夜交替、沙尘暴等动态地形效果。

**改动范围**：
- `models.py`：Tile 加 `status: str`（fire/frozen/sandstorm/none）
- `config.py`：动态效果常量
- `game_logic.py`：`apply_dynamic_terrain()`
- `web/style.css`：着火/冰面颜色动画

**状态**：[ ]

### ⏸ 需要确认的技术细节

1. 动态地形默认启用还是单独开关？
2. 冰面落水后单位直接死亡还是受伤？
3. 昼夜切换时前端需要什么视觉提示？

---

## P3.3 单位招募 + 经济系统

**概述**：开局不止 5 个单位，可在城堡/兵工厂招募新单位。

**改动范围**：
- `models.py`：Player 加 `gold`
- `config.py`：招募价格表
- `routes/game.py`：`POST /games/{id}/recruit` 端点
- `routes/turns.py`：end_of_turn 时 +100 金
- `web/app.js`：招募面板

**状态**：[ ]

### ⏸ 需要确认的技术细节

1. 是否限制最大单位数量？（建议：10/玩家）
2. 新单位生成位置在哪？（最近城堡/兵工厂旁边）
3. 是否限制每回合招募次数？

---

## P3.4 战斗动画层（纯代码）

**概述**：攻击/暴击/击杀/治疗时的 CSS+JS 动画反馈。

**改动范围**：
- `web/style.css`：攻击闪光/抖动 Keyframes
- `web/app.js`：`animateAttack()`、`animateCrit()`、`animateKill()`
- `web/index.html`：浮动数字容器

**状态**：[ ]

### ⏸ 需要确认的技术细节

1. 动画是否阻塞操作？（建议：不阻塞）
2. 动画是否可跳过？（建议：点击跳过）
3. 多段攻击播放一次动画还是多次？

---

# 📋 完整进度看板（2026-06-30 审计）

```
S0 ─────────────────────────────────────────────────────
S0.1 对话框系统         [✓]  → ⏸ 确认细节 → [✓]  → [✓]
S0.2 剧情系统            [✓]  → ⏸ 确认细节 → [✓]  → [✓]  (2026-06-27 4 步全完成)
S0.3 存档系统            [✓/🚧] 主线存档已完成；自由模式通用 SaveSlot 待做

P0 ─────────────────────────────────────────────────────
P0.1 敌我分阶段         [✓]  → ⏸ 确认细节 → [✓]  → [✓]  (2026-06-29 phase 字段 + 中文战报)
P0.2 战斗预测面板       [✓]  → ⏸ 确认细节 → [✓]  → [✓]
P0.3 反击系统           [✓]  → ⏸ 确认细节 → [✓]  → [✓]  (2026-06-30 反击伤害类型跟随反击方)
P0.4 地形增强+城市/工厂  [✓]  → ⏸ 确认细节 → [✓]  → [✓]  (2026-07-01 全 6 phase 完工)

P1 ─────────────────────────────────────────────────────
P1.1 武器耐久度         [   ]  → ⏸ 确认细节 → [   ] → [   ]
P1.2 兵种克制扩展        [   ]  → ⏸ 确认细节 → [   ] → [   ]
P1.3 晋升系统            [✓]  → ⏸ 确认细节 → [✓]  → [✓]  (XP 路线)

P2 ─────────────────────────────────────────────────────
P2.1 迷雾战争           [   ]  → ⏸ 确认细节 → [   ] → [   ]
P2.2 指挥官/CO Power    [   ]  → ⏸ 确认细节 → [   ] → [   ]
P2.3 地图任务多样化      [✓]  → ⏸ 确认细节 → [✓]  → [✓]  (2026-07-02 全 6 phase 完工)

P3 ─────────────────────────────────────────────────────
P3.1 支援系统            [   ]  → ⏸ 确认细节 → [   ] → [   ]
P3.2 地形动态            [   ]  → ⏸ 确认细节 → [   ] → [   ]
P3.3 招募+经济           [   ]  → ⏸ 确认细节 → [   ] → [   ]
P3.4 战斗动画层          [   ]  → ⏸ 确认细节 → [   ] → [   ]

🚧 进行中（非路线图项）
   - 生产 WebSocket 网关（替换 3s 轮询）
   - AI reaction 前端演出
   - 死代码清理（[`死代码.md`](死代码.md)）
   - 自定义地图编辑器前端 UI（API 已实装）
   - 主线剧情内容扩充（仅 chapter_01_steel_rebellion 一章）

里程碑:
  S0 ✓       = 可以写剧情讲故事了               ✅ 已达成
  P0 ✓       = 核心战斗完整                      ✅ 已达成（含魔攻魔防 + 术士 + 地形增强 + 经济 + claim + recruit）
  P1 + P2 ✓  = 可公开发布                        🚧 进行中（P2.3 胜利条件已完工，P1 武器/P2 迷雾/P2 CO Power 未做）
  P3 ✓       = 完整作品                          ⬜ 待启动

最新动向（2026-07-02 ~ 2026-07-03）：
  - PR #9 合并（YoukoSaint）：大厅内动态队伍编辑 PATCH /games/{id}/players/{pid}/team
  - 美术资产更新：经典棋子像素头像 game/app/web/assets/classic/ + 分割工具 split_classic_icons.py
  - 随机地图生成规范 docs/superpowers/specs/2026-07-01-random-map-generation-spec.md（592 行，待实现）
```

---

# 🔄 Agent 执行规则

1. **每步开始**：读取此文件，找第一个 `[ ]` 项
2. **确认前**：在聊天中输出「当前步骤：XXX」+ 该步骤的「⏸ 需要确认的技术细节」区块
3. **等待用户回答**后再开始编码
4. **完成后**：把 `[ ]` 改为 `[✓]`，commit message 注明步骤名
5. **不自动进入下一步**——等待用户指令
6. **每个 P 阶段全部 ✓ 后**：输出一份阶段总结

---

*最后更新: 2026-07-03*

*状态更新: P0.1 敌我分阶段已完成（commit `f539aed`，2026-06-29）；P0 整段达成（commit `014d6df` 加上物理/魔法双重战斗体系 + 术士兵种 + 中文 UI 词条，2026-06-30）；地形像素图生成工具（`tools/gen_terrain_tiles.py`，48×48 像素图）。*

*状态更新: P0.4 地形增强 + 经济 + claim + recruit (2026-07-01 完工) ◀ commit `e8853c6`…`f541017`；P2.3 地图任务多样化 + 队伍模式 (2026-07-02 完工) ◀ commit `d48a83b`…`1e25690`；PR #9 合并 (YoukoSaint) ： 大厅内 PATCH team + 棋子像素头像 （00f06f6）；随机地图生成规范 docs/superpowers/specs/2026-07-01-random-map-generation-spec.md（592 行，待实现）。

*GitHub: https://github.com/iris0heartmade/battleblitz*
